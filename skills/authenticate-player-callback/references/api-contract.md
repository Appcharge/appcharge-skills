# Authenticate Player Callback

Fetch the authoritative contract before implementing:

```bash
curl -fsSL 'https://docs.appcharge.com/api-reference/webstore/player-authentication/authenticate-player-callback.md'
```

OpenAPI (optional): https://docs.appcharge.com/api-reference/webstore/player-authentication/openapi-authenticate-player-callback.json
