# Grant Award Callback

Fetch the authoritative contract before implementing:

```bash
curl -fsSL 'https://docs.appcharge.com/api-reference/checkout/awards/grant-award-callback.md'
```

OpenAPI (optional): https://docs.appcharge.com/api-reference/checkout/awards/openapi-grant-award.json
