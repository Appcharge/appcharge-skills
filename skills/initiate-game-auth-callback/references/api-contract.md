# Initiate Game Auth Callback

Fetch the authoritative contract before implementing (Game Redirect Login only):

```bash
curl -fsSL 'https://docs.appcharge.com/api-reference/webstore/player-authentication/initiate-game-auth-callback.md'
```

OpenAPI (optional): https://docs.appcharge.com/api-reference/webstore/player-authentication/openapi-initiate-game-auth-callback.json
